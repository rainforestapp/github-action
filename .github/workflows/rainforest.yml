on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  rainforest:
    runs-on: ubuntu-latest
    name: Run Rainforest
    steps:
      - uses: actions/checkout@v4
      - name: Rainforest
        uses: ./
        with:
          token: ${{ secrets.RF_MAIN_API_TOKEN }}
          run_group_id: 9861
